name: Build SchoolQR APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Reconstruct full Android Studio project
        run: |
          cat ci/chunks/chunk00 ci/chunks/chunk01 ci/chunks/chunk02 ci/chunks/chunk03 ci/chunks/chunk04new ci/chunks/chunk05 ci/chunks/chunk06 ci/chunks/chunk07 | base64 -d > project.zip
          unzip -t project.zip
          unzip -q project.zip

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Install Android SDK packages
        run: |
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" --licenses >/dev/null || true
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" "platforms;android-35" "build-tools;35.0.0"

      - name: Build APK
        working-directory: SchoolQR_Studio
        run: gradle --no-daemon clean assembleDebug

      - name: Verify APK
        working-directory: SchoolQR_Studio
        run: |
          APK=app/build/outputs/apk/debug/app-debug.apk
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs "$APK"
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging "$APK" | head -30
          unzip -t "$APK"

      - name: Rename APK
        working-directory: SchoolQR_Studio
        run: cp app/build/outputs/apk/debug/app-debug.apk SchoolQR-Polished.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SchoolQR-Polished-APK
          path: SchoolQR_Studio/SchoolQR-Polished.apk
          if-no-files-found: error
